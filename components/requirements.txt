python-dotenv>=0.21.0
## components/requirements.txt
## Cleaned, deduplicated dependency list for component jobs.
## Keep this minimal to reduce conflicts with AzureML curated images.

# Core runtime libraries
python-dotenv>=0.21.0
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=1.0.0
joblib>=1.1.0
pydash>=5.0.0
requests>=2.28.0
tqdm
packaging
pyyaml>=6.0
deprecated
psutil

# Azure SDKs (minimal set used by deploy/register scripts)
azure-ai-ml>=1.7.0
azure-identity>=1.12.0
azure-core>=1.27.0
azure-keyvault-secrets>=4.7.0
azure-storage-blob>=12.13.0
azure-storage-file-datalake>=12.21.0
azure-storage-file-share>=12.22.0
azure-common
azure-mgmt-core
msal>=1.20.0
msal-extensions>=0.3.0

# Validation / YAML helpers
marshmallow>=3.5.0
strictyaml>=1.6.0

# Telemetry (optional)
opentelemetry-api
opentelemetry-sdk
opentelemetry-instrumentation-requests
opentelemetry-instrumentation-urllib3
opentelemetry-instrumentation-flask

# Note: If you run into dependency conflicts when installing at job start,
# prefer registering a reproducible environment (conda/Docker) with Azure ML.

